services:
  tests:
    build: .
    volumes:
      # Mount report dirs so HTML results are accessible on the host after the run
      - ./playwright-report:/app/playwright-report
      - ./test-results:/app/test-results
